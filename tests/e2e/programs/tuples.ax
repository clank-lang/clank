// Tuple operations
fn make_pair(a: Int, b: Int) -> (Int, Int) {
  (a, b)
}

fn swap(pair: (Int, Int)) -> (Int, Int) {
  let (x, y) = pair;
  (y, x)
}

fn main() -> Int {
  let p = make_pair(10, 20);
  let (a, b) = swap(p);
  a + b
}
